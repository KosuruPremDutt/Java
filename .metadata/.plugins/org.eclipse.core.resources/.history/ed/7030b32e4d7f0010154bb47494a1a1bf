package com.doctorrbooking.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.doctorbooking.model;

public interface BookingRepository extends JpaRepository<Booking, Long> {
    
}
